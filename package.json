{
  "name": "weixin-claude-code",
  "version": "0.3.0",
  "description": "WeChat Channel for Claude Code via iLink Bot API",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "bun build src/index.ts --outfile dist/index.js --target bun",
    "start": "bun install --no-summary && bun dist/index.js",
    "dev": "tsc --watch",
    "typecheck": "tsc --noEmit",
    "test": "bun test"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.27.1",
    "qrcode-terminal": "0.12.0",
    "silk-wasm": "^3.7.1",
    "zod": "4.3.6"
  },
  "devDependencies": {
    "@types/node": "^25.5.0",
    "@types/qrcode-terminal": "^0.12.2",
    "typescript": "^5.9.3"
  }
}
